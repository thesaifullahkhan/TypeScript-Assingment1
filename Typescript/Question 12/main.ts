const names: string[] = ["Afnaan", "Qadir", "Hassan", "Faizan"];

// Print a personalized message for each person
for (let i = 0; i < names.length; i++) {
  const message: string = `Hello, ${names[i]}! Have a great day.`;
  console.log(message);
}
